---
// ShowcaseItem.astro
// One item in the "waste to art" showcase — image + title + short caption.
// Used 3 times on the Trifolium page with different props.

interface Props {
  image: string;
  title: string;
  caption: string;
}

const { image, title, caption } = Astro.props;
---

<div class="showcase-item">
  <div class="showcase-photo">
    <img src={image} alt={title} />
  </div>
  <h4 class="showcase-title">{title}</h4>
  <p class="showcase-caption">{caption}</p>
</div>

<style>
  .showcase-item {
    display: flex;
    flex-direction: column;
    gap: 8px;
    flex: 1 1 260px;
  }

  .showcase-photo {
    background: white;
    aspect-ratio: 4 / 3;
    overflow: hidden;
  }

  .showcase-photo img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .showcase-title {
    font-weight: 700;
    font-size: 18px;
  }

  .showcase-caption {
    font-size: 15px;
    color: var(--color-grey);
    line-height: 1.4;
  }
</style>
